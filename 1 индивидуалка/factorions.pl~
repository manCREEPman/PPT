fact(N, _):- N < 0, !.
fact(0, 1).
fact(1, 1).
fact(N, X):- fact(N, 1, 1, X).
fact(N, N, Mul, Mul):-!.
fact(N, I, Mul, X):- Mul1 is Mul * (I + 1), I1 is I + 1, fact(N, I1, Mul1, X).



sumEachNumberFact(Value, Res).
